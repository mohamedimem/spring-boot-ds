{{ form_start(form) }}
    {{ form_row(form.matricule) }}  <!-- Manually render the matricule field -->

{{ form_row(form.nom) }}
    {{ form_row(form.prenom) }}

<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
