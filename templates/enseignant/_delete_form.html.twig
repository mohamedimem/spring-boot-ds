<form method="post" action="{{ path('app_enseignant_delete', {'matricule': enseignant.matricule}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enseignant.matricule) }}">
    <button class="btn">Delete</button>
</form>
