<form method="post" action="{{ path('app_soutenance_delete', {'numjury': soutenance.numjury}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soutenance.numjury) }}">
    <button class="btn">Delete</button>
</form>
